# apphosting.yaml
runConfig:
  cpu: 2
  memory: 2GB
  minInstances: 0
  maxInstances: 10

env:
  - variable: NODE_ENV
    value: production
  - variable: NEXT_TELEMETRY_DISABLED
    value: "1"

buildConfig:
  commands:
    - npm ci
    - npm run build